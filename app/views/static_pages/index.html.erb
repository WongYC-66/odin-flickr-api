<h1>Odin Flickr Finder</h1>

<%= form_with url: "/search", method: :get do |form| %>
  <%= form.label :id, "Flickr Id:" %>
  <%= form.search_field :id, {value:"201753371@N02"} %>
  <%= form.submit "Search", {name:nil} %>
<% end %>

<p>some flickr_id u could search are :</p>
<ul>
  <li>naturewatch1
  <li>chm
  <li>adecoleman
  <li>anna_soffia
</ul>

<% if @photos %>
<h2>Flickr Search Result:</h2>
  <% @photos.each do |photo| %>
    <%= image_tag "https://live.staticflickr.com/#{photo["server"]}/#{photo["id"]}_#{photo["secret"]}.jpg" %>
  <% end %>
<% end %>